apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: my-storage
  volumeMode: Filesystem
  accessModes: 
  - ReadWriteMany

